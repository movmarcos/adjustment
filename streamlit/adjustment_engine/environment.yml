name: adjustment-engine
channels:
  - snowflake
dependencies:
  # 1.50.0 — the same runtime as the RAPTOR BAU Operations app on this
  # account (Marcos, 2026-09-08). Brings the native st.dataframe toolbar
  # (search / CSV download / fullscreen), column_config, hide_index, row
  # selection and st.container(border=True). The app keeps its API guards so
  # it still degrades on older runtimes.
  - streamlit=1.50.0
  - plotly
